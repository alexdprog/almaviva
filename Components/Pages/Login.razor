@page "/login"
@inject NavigationManager Navigation

<FluentCard>
    <p>Use centralized OAuth login.</p>
    <FluentButton Appearance="Appearance.Accent" OnClick='() => Navigation.NavigateTo("/auth/login", true)'>Login via OAuth2</FluentButton>
</FluentCard>
